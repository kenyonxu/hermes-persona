# PLAN-008: JSON 文件位置整顿实施计划

> 基于 SPEC-008，7 个 Phase，45 分钟完成。

---

## Phase 0 — 前置检查（5 分钟）

**目标：** 确认基线 + 目录存在。

| # | 检查项 | 命令 | 预期 |
|---|--------|------|------|
| 0.1 | 测试基线 | `python -m pytest tests/ -q` | 333 passed |
| 0.2 | keywords/ 存在 | `ls keywords/` | 7 个 JSON |
| 0.3 | locales/ 存在 | `ls locales/` | en.json + zh.json |
| 0.4 | state/ 不存在 | `ls state/ 2>&1` | 不存在（将自动创建） |

**风险：** 低 | **回滚：** 无需回滚

---

## Phase 1 — config.py：提取公共路径函数（10 分钟）

**目标：** 消除 `injector.py` 和 `guard.py` 中的重复路径逻辑。

### 改动：`config.py`（当前 7 行 → ~30 行）

```python
# 在 _CONFIG_ROOT 定义后新增：

def _resolve_config_path(filename: str = "persona-config.json") -> Path | None:
    """三层 fallback 解析配置文件路径。

    1. 插件目录（新规范）→ Path(__file__).parent / filename
    2. _CONFIG_ROOT（旧规范，profile 根目录）
    3. 返回 None（调用方自行处理）
    """
    # L1: 插件目录（__file__ 是 config.py 所在目录 = 插件根）
    plugin_path = Path(__file__).resolve().parent / filename
    if plugin_path.is_file():
        return plugin_path

    # L2: _CONFIG_ROOT（旧规范，向后兼容）
    if _CONFIG_ROOT is not None:
        legacy_path = _CONFIG_ROOT / filename
        if legacy_path.is_file():
            return legacy_path

    return None
```

### 改动：`injector.py` L106-131 `_load_config()`

**旧：**
```python
if _config._CONFIG_ROOT is not None:
    config_path = _config._CONFIG_ROOT / "persona-config.json"
else:
    config_path = Path(__file__).resolve().parents[2] / "persona-config.json"
```

**新：**
```python
config_path = _config._resolve_config_path("persona-config.json")
if config_path is None:
    # L3 fallback: repo 根目录（pytest 等场景）
    config_path = Path(__file__).resolve().parents[2] / "persona-config.json"
```

### 改动：`guard.py` L24-51 `_load_guard_config()`

**旧：** 复制了相同的 `_CONFIG_ROOT` / `parents[2]` 逻辑

**新：**
```python
import config as _config
config_path = _config._resolve_config_path("persona-config.json")
if config_path is None:
    config_path = Path(__file__).resolve().parents[2] / "persona-config.json"
```

**风险：** 中 | **回滚：** `git checkout -- config.py injector.py guard.py`

**验证：**
```bash
python -m pytest tests/test_injector.py tests/test_guard.py -q
```

---

## Phase 2 — expression_vector.py：默认存储路径迁移（10 分钟）

**目标：** `storage_path` 默认值从 `~/.hermes/` 迁移到插件 `state/` 目录。

### 改动：`expression_vector.py` L80-85

**旧：**
```python
raw_path = cfg.get(
    "storage_path", "~/.hermes/expression_vector.json"
)
```

**新：**
```python
raw_path = cfg.get(
    "storage_path",
    str(Path(__file__).resolve().parent / "state" / "expression_vector.json"),
)
```

> **向后兼容：** 用户如果在 `persona-config.json` 里显式指定了 `storage_path`，不会被覆盖。只有未配置时才走新默认值。

### 同步：`examples/persona-config.json` L101

**旧：** `"storage_path": "~/.hermes/profiles/{profile}/state/expression_vector.json"`

**新：** `"storage_path": ""`（注释说明默认路径为插件目录 `state/`，空字符串表示使用默认）

**风险：** 中 | **回滚：** `git checkout -- expression_vector.py examples/persona-config.json`

**验证：**
```bash
python -m pytest tests/test_expression_vector.py -q
# 确认新 state/ 目录在插件根目录下被创建
ls -la state/
```

---

## Phase 3 — injector.py：daily_turn_count 默认路径迁移（5 分钟）

**目标：** `_daily_turn_count_hint()` 的默认存储路径从 profile 迁移到插件 `state/`。

### 改动：`injector.py` L771-774

**旧：**
```python
raw_path = dc_cfg.get(
    "storage_path",
    "~/.hermes/profiles/{profile}/state/daily_turn_count.json",
)
```

**新：**
```python
raw_path = dc_cfg.get(
    "storage_path",
    str(Path(__file__).resolve().parent / "state" / "daily_turn_count.json"),
)
```

### 改动：`injector.py` L1032（debug 详细模式）

**旧：**
```python
dc_path_raw = dc_cfg.get("storage_path", "~/.hermes/profiles/{profile}/state/daily_turn_count.json")
```

**新：**
```python
dc_path_raw = dc_cfg.get("storage_path", str(Path(__file__).resolve().parent / "state" / "daily_turn_count.json"))
```

**风险：** 低 | **回滚：** `git checkout -- injector.py`

**验证：**
```bash
python -m pytest tests/test_fixed_signals.py -q
```

---

## Phase 4 — 清理 + .gitignore（5 分钟）

**目标：** 删除废弃文件，保护运行时生成文件不入版本控制。

### 改动：`~/github/hermes-persona/.gitignore`

追加：
```
# Runtime state (auto-generated by plugin)
state/
```

### 删除文件：

| 文件 | 原因 |
|------|------|
| `~/.hermes/profiles/zhihui/persona-config (副本).json` | 旧备份 |
| `~/.hermes/profiles/zhihui/persona-config.json` | 迁移到插件目录后，旧位置废弃 |

> ⚠️ 删除前确认：插件目录下已有 `persona-config.json`（从旧位置复制/移动）

### 迁移步骤（部署时手动执行）：

```bash
# 1. 复制活跃配置到插件目录
cp ~/.hermes/profiles/zhihui/persona-config.json \
   ~/.hermes/profiles/zhihui/plugins/hermes-persona/

# 2. 确认插件目录下文件存在且内容正确
diff ~/.hermes/profiles/zhihui/persona-config.json \
     ~/.hermes/profiles/zhihui/plugins/hermes-persona/persona-config.json

# 3. 删除旧位置
rm ~/.hermes/profiles/zhihui/persona-config.json
rm ~/.hermes/profiles/zhihui/"persona-config (副本).json"
```

**风险：** 低 | **回滚：** `git checkout -- .gitignore`，旧文件可从 git 历史恢复

---

## Phase 5 — 测试更新（5 分钟）

**目标：** 新增路径 fallback 测试，更新 conftest fixture。

### 改动：`tests/conftest.py`

在 `temp_config_root` fixture 中创建 `plugins/hermes-persona/` 子目录结构，模拟新文件布局：

```python
# 在临时目录下创建插件子目录
plugin_dir = tmp_path / "plugins" / "hermes-persona"
plugin_dir.mkdir(parents=True)
(plugin_dir / "keywords").mkdir(exist_ok=True)
(plugin_dir / "state").mkdir(exist_ok=True)
# 复制 keywords JSON 到临时插件目录
```

### 新增：`tests/test_config_paths.py`

7 个测试用例：

| 用例 | 场景 | 预期 |
|------|------|------|
| LOC-01 | 插件目录有 config → 优先加载 | 返回插件目录的配置 |
| LOC-02 | 插件目录无，_CONFIG_ROOT 有 → fallback | 返回 profile 根目录的配置 |
| LOC-03 | 两处都没有 → 返回 {} | 优雅降级 |
| LOC-04 | keywords/ 在插件目录下 → jieba 正常分词 | 匹配成功 |
| LOC-05 | state/ 自动创建 → expression_vector.json 写入 | 文件存在 |
| LOC-06 | state/ 自动创建 → daily_turn_count.json 写入 | 文件存在 |
| LOC-07 | 用户显式 storage_path 不覆盖 | 使用用户指定的路径 |

**风险：** 中 | **回滚：** `git checkout -- tests/conftest.py && rm tests/test_config_paths.py`

---

## Phase 6 — 全量验收（5 分钟）

**目标：** 全量测试通过 + 手动确认文件位置。

| # | 验收项 | 命令 | 预期 |
|---|--------|------|------|
| 6.1 | 全量测试 | `python -m pytest tests/ -q` | 340+ passed |
| 6.2 | 插件目录结构 | `ls plugins/hermes-persona/{state,keywords,locales,persona-config.json}` | 全部存在 |
| 6.3 | 旧位置清理 | `ls ~/.hermes/profiles/zhihui/persona-config.json` | 不存在 |
| 6.4 | 向后兼容 | 手动设 `_CONFIG_ROOT=/old/path` 后运行测试 | fallback 正常 |

**风险：** 低 | **回滚：** 无需回滚（验收阶段）

---

## 总计

| Phase | 内容 | 时间 | 风险 |
|-------|------|------|------|
| 0 | 前置检查 | 5 分钟 | 低 |
| 1 | config.py 公共函数 + injector/guard 统一 | 10 分钟 | 中 |
| 2 | expression_vector 默认路径 | 10 分钟 | 中 |
| 3 | daily_turn_count 默认路径 | 5 分钟 | 低 |
| 4 | 清理 + .gitignore | 5 分钟 | 低 |
| 5 | 测试更新 | 5 分钟 | 中 |
| 6 | 全程验收 | 5 分钟 | 低 |
| **合计** | | **45 分钟** | |

---

## 关键决策

1. **双层 fallback，不碰 pytest ci：** 测试中 `_CONFIG_ROOT` 仍然指向临时目录，L1 插件目录不存在时自动 fallback 到 L2/L3，测试无需大改
2. **state/ 进 .gitignore：** 运行时生成文件不入版本控制
3. **配置迁移手动执行：** 从 profile 根 → 插件目录的复制由部署脚本/手动完成，代码层只保证路径解析正确
